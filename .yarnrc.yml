yarnPath: .yarn/releases/yarn-3.2.1.cjs

# Turbopack/esbuild/CDK tooling here isn't verified against Yarn PnP —
# node_modules keeps everything working exactly as it did under yarn classic.
nodeLinker: node-modules
